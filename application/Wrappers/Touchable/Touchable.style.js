import {StyleSheet} from 'react-native';

export default styles = StyleSheet.create({
  buyButton: {
    fontWeight: 'bold',
    fontSize: 20,
    color: '#ed6565',
    alignItems: 'center',
    justifyContent: 'center',
    padding: 5,
  }
})
